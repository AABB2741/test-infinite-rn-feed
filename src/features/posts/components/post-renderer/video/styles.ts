import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    width: "100%",
    gap: 24,
    overflow: "hidden",
  },
  contentAuthor: {},
  video: {
    width: "100%",
    minHeight: 1,
    flexShrink: 1,
  },
});
