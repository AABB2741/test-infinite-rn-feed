import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 24,
    flexDirection: "row",
    alignItems: "center",
    gap: 12,
  },
  avatar: {
    width: 25,
    height: 25,
    borderRadius: 25,
  },
  name: {
    flex: 1,
  },
});
